"""External integrations that require third-party credentials.

These modules define clean interfaces and are intentionally NOT wired into the
running app, because they need paid accounts / OAuth secrets to function. They
document exactly what to plug in. Nothing here fabricates results.
"""
